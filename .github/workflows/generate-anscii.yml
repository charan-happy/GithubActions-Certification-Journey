name: a workflow to generate ASCII Art work

on: push

jobs:
    anscii_job:
        runs-on: ubuntu-latest
        steps:
            - name: checkout repo
              uses: actions/checkout@v4

            - name: checkout repo files 
              run: ls -lrta

            - name: Executing shell script
              run: |
                chmod +x ./anscii-script.sh
                ./anscii-script.sh
                

